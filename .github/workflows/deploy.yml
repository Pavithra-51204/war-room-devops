name: CI/CD with Infisical

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Install Infisical
      run: |
        curl -1sLf 'https://dl.cloudsmith.io/public/infisical/infisical-cli/setup.deb.sh' | sudo -E bash
        sudo apt-get install infisical -y

# 3. Fetch Secrets & Surgical Clean
    - name: Fetch Secrets
      env:
        INFISICAL_TOKEN: ${{ secrets.INFISICAL_TOKEN }}
      run: |
        # Export secrets
        infisical export --env=dev --format=dotenv > .env
        
        # This removes all " and ' characters from the file so Docker gets the RAW string
        sed -i 's/["'\'']//g' .env
        
        # Load into GitHub Environment
        cat .env >> $GITHUB_ENV

    # 4. Login to Docker Hub (Simplified)
    - name: Docker Login
      run: |
        # We remove the extra quotes here too to be 100% sure
        echo ${{ env.DOCKER_PASSWORD }} | docker login -u ${{ env.DOCKER_USERNAME }} --password-stdin
    - name: Build and Push
      run: |
        docker build -t ${{ env.DOCKER_USERNAME }}/war-room-backend ./server
        docker push ${{ env.DOCKER_USERNAME }}/war-room-backend

# 6. Deploy to EC2
    - name: SSH Deploy
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ env.EC2_HOST }}
        username: ${{ env.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }} # Uses the secret we just added to GitHub
        script: |
          # Use raw variables for the EC2 commands
          echo "${{ env.DOCKER_PASSWORD }}" | docker login -u "${{ env.DOCKER_USERNAME }}" --password-stdin
          docker pull ${{ env.DOCKER_USERNAME }}/war-room-backend
          docker stop warroom || true
          docker rm warroom || true
          docker run -d -p 4000:4000 --env-file .env --name warroom ${{ env.DOCKER_USERNAME }}/war-room-backend